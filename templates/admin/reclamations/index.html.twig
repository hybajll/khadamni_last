{% extends 'base.html.twig' %}

{% block title %}Admin - Réclamations{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
        <div>
            <h1 class="h4 mb-1 text-darken">Réclamations</h1>
            <div class="text-muted">Espace modérateur (starter).</div>
        </div>
        <a href="{{ path('app_admin_dashboard') }}" class="btn btn-secondary">Retour</a>
    </div>

    <div class="card">
        <div class="card-body p-4">
            <div class="text-muted">À implémenter : liste + réponse aux réclamations.</div>
        </div>
    </div>
</div>
{% endblock %}

