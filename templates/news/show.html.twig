{% extends 'base.html.twig' %}

{% block title %}News{% endblock %}

{% block body %}
    <h1>News</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ news.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ news.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ news.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ news.createdAt ? news.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ImageUrl</th>
                <td>{{ news.imageUrl }}</td>
            </tr>
            <tr>
                <th>IsExternal</th>
                <td>{{ news.isExternal ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>SourceLink</th>
                <td>{{ news.sourceLink }}</td>
            </tr>
            <tr>
                <th>AuthorType</th>
                <td>{{ news.authorType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_news_index') }}">back to list</a>

    <a href="{{ path('app_news_edit', {'id': news.id}) }}">edit</a>

    {{ include('news/_delete_form.html.twig') }}
{% endblock %}
